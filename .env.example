# --- Required ---
# Supabase / Postgres connection string (must have the pgvector extension available).
DATABASE_URL=postgresql://user:password@host:5432/postgres

# Password for the /admin dashboard.
ADMIN_PASSWORD=change-me-to-something-long

# Secret used to sign admin session cookies. Generate one, e.g.:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=change-me-to-a-long-random-value

# Optional: enable the read-only share page at /rules/<SHARE_TOKEN> so teammates
# who can't connect over MCP can still view/copy the current rules. Leave empty to
# disable. Generate one: python -c "import secrets; print(secrets.token_urlsafe(24))"
SHARE_TOKEN=

# --- Optional (sane defaults shown) ---
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
PORT=8080

# Retrieval / storage tuning (see resolved decisions #2 and #3).
MIN_OBSERVATION_LENGTH=15
DEDUP_THRESHOLD=0.9
RELEVANCE_FLOOR=0.25
DEFAULT_RECALL_LIMIT=5
MAX_RECALL_LIMIT=25
